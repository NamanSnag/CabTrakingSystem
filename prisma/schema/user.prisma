model User {
    id        Int      @id @default(autoincrement())
    uuid      String   @unique
    email     String   @unique
    name      String
    image     String?
    password  String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt
}
